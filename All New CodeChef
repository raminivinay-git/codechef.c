#include <stdio.h>

int main() {
    int x, y;
    scanf("%d %d", & x, & y);
    if (x > y) {
        printf("NEW");
    }
    else if (x < y) {
        printf("OLD");
    }
    else {
        printf("SAME");
    }
    return 0;
}
